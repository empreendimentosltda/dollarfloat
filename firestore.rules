rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only the authenticated server (Admin SDK / IAM) accesses store data.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
